{
  "name": "completogiulio",
  "version": "1.0.0",
  "description": "Backend wrapper for Railway deployment",
  "type": "module",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "install": "cd backend && npm install",
    "build": "cd backend && npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}
